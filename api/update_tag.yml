#更新已创建的标签
name: update_tag api
base_url: ${ENV(BASE_URL)}
request:
  method: POST
  url: /cgi-bin/tags/update
  params:
    access_token: $token_id
  json: {   "tag" : { "id" : 107  ,  "name" : "44"   } }
validate:
    - eq: ['status_code',200]


